{
  "name": "zhaiquant-tool-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Offline tests for the read-only TDX capture helper",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "test": "node --test tests/test_tdx_capture.mjs"
  },
  "devDependencies": {
    "sharp": "0.35.4"
  }
}
